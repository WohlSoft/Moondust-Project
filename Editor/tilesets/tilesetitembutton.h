#ifndef TILESETITEMBUTTON_H
#define TILESETITEMBUTTON_H

#include <QFrame>

class TilesetItemButton : public QFrame
{
    Q_OBJECT
public:
    explicit TilesetItemButton(QObject *parent = 0);

signals:

public slots:

};

#endif // TILESETITEMBUTTON_H
